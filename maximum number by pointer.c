#include<stdio.h>
int main()
{
	int first,second,*p,*q;
	printf("enter the first number:");
	scanf("%d",&second);
	printf("enter the second number:");
	scanf("%d",&second);
	p=&first;
	q=&second{
	printf("maximun number is %d",*p);
	}
	else
	printf("maximum number is %d",*q);
}
